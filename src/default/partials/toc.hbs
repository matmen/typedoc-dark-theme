<li class="{{#if isInPath}}current{{/if}} {{cssClasses}}">
    <a href="{{relativeURL url}}" class="tsd-kind-icon">{{{title}}}</a>
    {{#if children}}
        <ul>
            {{#each children}}
                {{> toc}}
            {{/each}}
        </ul>
    {{/if}}
</li>
